# reference/

- `kdp_report.html` — the original self-contained single-page report (2009–2025). This is the
  **design source of truth**: the visual style, the interactive H2H tool, the Record Book, and
  all hardcoded tables to port into the new multi-page site. No external dependencies (inline
  SVG/JS). Open in any browser to see the target look and feel.
